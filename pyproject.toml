[project]
name = "coins"
version = "0.1.0"
description = "Vyper implementation of the singleton multitoken design"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mamushi>=0.1.0",
    "moccasin>=0.4.3",
    "ruff>=0.15.9",
    "snekmate>=0.1.2",
]
